<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_marginHorizontal="@dimen/tv_margin"
    android:background="@color/white">

    <ImageView
        android:id="@+id/map_head_image"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/map"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toBottomOf="@id/map_head_image"
        android:orientation="vertical"
        android:layout_marginHorizontal="@dimen/padding_medium">

        <Button
            android:id="@+id/google_sign_in_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sign In with Google"
            android:textColor="@color/white"
            android:drawableLeft="@drawable/icons_google"
            android:layout_marginVertical="@dimen/tv_margin"
            />
        <Button
            android:id="@+id/email_sign_in_btn"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Sign In with Email"
            android:textColor="@color/white"
            android:drawableLeft="@drawable/icons_gmail"
            />
    </LinearLayout>
</androidx.constraintlayout.widget.ConstraintLayout>